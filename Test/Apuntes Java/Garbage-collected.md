Mecanismo automático de gestión de memoria, libera memoria ocupada por objetos que ya no son utilizados, que no tienen referencias activas.
Gestiona la memoria del heap (zona de memoria dinámica donde se almacenan objetos y datos que se crean en tiempo de ejecución), donde se almacenan los objetos creados con *new*.

Inventado por John McCarthy em 1959.